[package]
name = "ofdrw-compat"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Compatibility tests migrated from the ofdrw project (fixtures, parsing assertions, and rendering comparisons)."

[dependencies]

[dev-dependencies]
cairo-rs = { workspace = true, features = ["png"] }
image = { workspace = true }
rofd-core = { path = "../../crates/rofd-core" }
rofd-render = { path = "../../crates/rofd-render" }
